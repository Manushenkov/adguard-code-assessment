export const NEWLINE_REGEX = /\r?\n/; // \r is for carriage return on windows format. Some files have \r\n as newline characters

export const SUBDOCUMENT = "$subdocument";
export const DOCUMENT = "$document";
